# InkMOD branding update
- Added `src/images/InkMODLogo240.h`: full logo already containing the inkmod wordmark.
- Boot screen now uses the full logo and no longer prints `InkMOD` below it.
- Default sleep screen now uses the full logo and no duplicate text.
- Portrait Calendar uses the full logo and no duplicate text.
- Landscape Calendar keeps the compact 120px mark for layout/rotation safety, but duplicate `InkMOD` text is removed.
- Web `/logo.png` is unchanged in this patch because it uses a separate color/PNG asset pipeline.
