inkMOD 1.1.4 Stable
===================

This archive was prepared from the hardware-tested source supplied on 2026-08-14.
Runtime/reader/FB2/cover/SD-card/web/font-fallback logic was not changed during release packaging.

Release configuration:
- PlatformIO default environment: release
- Firmware variant: release (no "developer" label)
- LOG_LEVEL=-1
- ENABLE_SERIAL_LOG is not enabled in the release environment
- USE_NEW_READER=0 (same tested reader path)

Build:
  BUILD_RELEASE.bat
or:
  set INKMOD_RELEASE_VERSION=1.1.4
  pio run -e release -t clean
  pio run -e release

Expected versioned artifact:
  .pio\build\release\firmware-release-v1.1.4.bin

Important: this packaging step did not rebuild the firmware in ChatGPT's environment because PlatformIO is not installed there. Build it on the same PC/toolchain used for the successful hardware-tested developer build.
